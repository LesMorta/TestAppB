<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TestAppB.Models"
             x:Class="TestAppB.Views.MyPlantsPage">
    
    <StackLayout Padding="15">

        <Button Text="Добавить растение" Clicked="AddPlant_Clicked" BackgroundColor="#66bb6a" TextColor="White"/>

        <CollectionView x:Name="plantsList" SelectionMode="None" Margin="0,10,0,0">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <SwipeView>
                        <SwipeView.RightItems>
                            <SwipeItems>
                                <SwipeItem Text="Удалить" BackgroundColor="Red" Invoked="DeletePlant_Invoked" />
                            </SwipeItems>
                        </SwipeView.RightItems>

                        <Grid Padding="10" ColumnDefinitions="*, Auto">

                            <Label Text="{Binding Name}" FontSize="18" VerticalOptions="Center">
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer NumberOfTapsRequired="1"
                              CommandParameter="{Binding .}"
                              Tapped="OnPlantTapped" />
                                </Label.GestureRecognizers>
                            </Label>

                            <Button Text="{Binding IsWatered, Converter={StaticResource StatusToTextConverter}}" 
                                    CommandParameter="{Binding .}" 
                                    Clicked="ToggleWatered_Clicked"
                                    Grid.Column="1" />
                        </Grid>
                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </StackLayout>
    
</ContentPage>